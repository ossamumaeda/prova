A validação de um unico endereço por pessoa será feita em nivel de service e API, não em nivel de modelagem do banco.

Detalhes sobre a exclusão de registros. O campo de "ativo" em ambas a tabelas de pessoa e endereco serve para evitar excluir fisicamente registros das tabelas. 

Detalhes sobre a escolha de idioma nos nomes de variaveis e arquivos. Por padrão os nomes são definidos em ingles como linguagem universal, mas como foi deixado explicito nos Detalhes
de implementação do desafio, os campos do banco de dados e nome da tabela estão em portugues.

Decisão de projeto: Usar ou não interfaces na camada de service. Como é uma aplicação relativamente simples, sem a necessidade de uma possível troca de banco de dados,
tomei a decisão de não abstrair a camada de service, como é comumente vista em projetos java, visando evitar "overengineering". Ou seja, ao invés de:
public interface PessoaService{} -> Onde as inscrições das funções definidas
e depois 
public class PessoaServiceImplementation implements PessoaService{} -> Onde ocorre as implementações das funções

Implementei apenas public class PessoaService{}

Detalhes sobre o remove de pessoa: Como o sistema está utilizando o remove lógico, caso uma pessoa seja "removida", a flag ativo é settada como false e consequentemente os endereços
atrelados a pessoa ficam com a flag ativo false tambem.

Detalhes sobre o update de pessoa e endereço: Visando as diferentes funcionalidades do front-end, existem 3 usecases diferentes, um para editar apenas pessoa, editar apenas endereço
e editar pessoa + endereço.